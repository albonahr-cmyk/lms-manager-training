# sample.mp4 について

このファイルは **モック用のダミー動画ファイル** です (Phase 2a)。

- 現在は 0 バイトの空ファイルが置かれています。
- 動画再生 UI を確認したい場合は、任意の mp4 ファイル (H.264 + AAC 推奨) を `public/sample.mp4` として上書き配置してください。
- 本番 (Phase 4) では Vercel Blob (private) からの署名付き URL に切り替えます。`Lesson.videoUrl` の値も blob URL に置き換える想定です。
